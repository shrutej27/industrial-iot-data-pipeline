# OPC-UA Server

Async OPC-UA server exposing machine status (running/idle/fault) and energy consumption.

## Port

- `4840` (OPC-UA default)

## Namespace

- `MachineStatus` — running / idle / fault per machine
- `EnergyConsumption` — kWh readings per machine
